cpu_arch_alt_name:
  aarch64: "arm64"
  x86_64: "amd64"
ghostty: true
ghostty_ctrl_key: "{{ 'super' if ansible_facts['distribution'] == 'MacOSX' else 'ctrl' }}"
ghostty_super_key: "{{ 'ctrl' if ansible_facts['distribution'] == 'MacOSX' else 'super' }}"
helmls_version: "0.5.4"
nodejs_package_name: "{{ 'nodejs-npm' if ansible_facts['distribution'] == 'Fedora' else 'npm' }}"
stylua_version: "2.3.1"
work: false
yamlfmt_version: "0.21.0"
zellij: false
zellij_version: "0.41.2"
